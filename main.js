// Função preload para carregar recursos (pode ser deixada vazia)
function preload() {
    // Deixe vazio ou adicione carregamento de recursos aqui
}

// Função setup para configurar a tela
function setup() {
    // Crie a tela e coloque-a no centro
    createCanvas(windowWidth, windowHeight);
    background(255);
}

// Função draw (pode ser deixada vazia)
function draw() {
    // Deixe vazio ou adicione desenho personalizado aqui
}

// Função para salvar a imagem com filtro
function salvarImagemComFiltro() {
    // Lógica para salvar a imagem com filtro aqui
    // Por exemplo, você pode usar p5.js para aplicar um filtro à imagem e salvar
}